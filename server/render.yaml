services:
  - type: web
    name: visualspam-garden-api
    runtime: node
    buildCommand: cd .. && npm install && cd server && npm install && cd .. && npm run build
    startCommand: cd .. && node server/index.js
    envVars:
      - key: MONGO_URI
        sync: false
      - key: NODE_ENV
        value: production
