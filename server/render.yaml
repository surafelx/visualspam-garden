services:
  - type: web
    name: visualspam-garden-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: NODE_ENV
        value: production
      - key: SERVE_CLIENT
        value: false
