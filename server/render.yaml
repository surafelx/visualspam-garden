services:
  - type: web
    name: visualspam-garden-api
    runtime: node
    buildCommand: npm install && cd .. && npm install && npm run build && cd server
    startCommand: cd .. && node server/index.js
    envVars:
      - key: MONGO_URI
        sync: false
      - key: NODE_ENV
        value: production
